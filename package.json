{
  "name": "rv-learning-hub-lms",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "RV Learning Hub LMS — Full-stack Learning Management System (Web + Mobile + Dashboard)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "cd backend && node index.js",
    "server:dev": "cd backend && npx nodemon index.js",
    "test": "cd backend && npm test",
    "start": "concurrently \"npm run server\" \"npm run dev\""
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
